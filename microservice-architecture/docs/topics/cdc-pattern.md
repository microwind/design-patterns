# CDC Pattern

## 场景问题

很多系统希望把数据库中的数据变更传播给搜索、数仓或事件系统，但又不想在业务代码里硬编码一堆同步调用。

## 核心思想

CDC 的目标是：

- 从变更日志中捕获数据变化
- 由 connector 扫描未处理变更
- 转换成事件并发布到下游

## 最小可运行示例

统一模型：

1. 写入一条订单变更日志
2. CDC connector 读取未处理变更
3. 发布到 broker 并标记已处理

## 常见误区

1. 把 CDC 和 outbox 完全等同
2. 只关注发布，不关注重复消费
3. 对变更类型没有标准化建模
