# Circuit Breaker

## 场景问题

当下游持续失败时，如果调用方还不断继续请求，只会把故障继续放大。

## 核心思想

断路器的目标是：

- 正常时放行
- 连续失败达到阈值后打开断路器
- 打开后快速失败
- 经过一次试探调用成功后恢复关闭

## 最小可运行示例

统一模型：

1. 初始 `closed`
2. 连续失败后进入 `open`
3. 试探通过进入 `half-open`
4. 成功后恢复 `closed`

## 常见误区

1. 只有 open/closed，没有恢复探测
2. 与重试组合后形成自我打架
3. 不记录状态机变化
