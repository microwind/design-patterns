# Retry Pattern

## 场景问题

远程调用失败并不总是永久失败，很多错误只是瞬时抖动。

但如果完全不重试，系统容易被偶发失败放大；如果无脑重试，又会把故障放大成风暴。

## 核心思想

重试模式的核心是：

- 只对可恢复错误重试
- 设最大次数
- 成功则立即停止
- 失败则明确结束

## 最小可运行示例

统一模型：

1. 前两次调用失败
2. 第三次成功
3. 若最大次数不足，则最终失败

## 常见误区

1. 所有错误都重试
2. 没有上限
3. 与幂等设计脱节
