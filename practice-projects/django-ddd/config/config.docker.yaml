# Docker 环境下使用：host 指向 compose service name

server:
  host: "0.0.0.0"
  port: 8080
  debug: false

database:
  user:
    engine: "mysql"
    host: "mysql"
    port: 3306
    name: "frog"
    username: "frog_admin"
    password: "frog798"
    conn_max_age: 60
  order:
    engine: "postgres"
    host: "postgres"
    port: 5432
    name: "seed"
    username: "postgres"
    password: "lego798"
    conn_max_age: 60

logger:
  level: "INFO"

event:
  publisher: "memory"
