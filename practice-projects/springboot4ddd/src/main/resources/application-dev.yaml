# Development Environment
spring:
  mvc:
    throw-exception-if-no-handler-found: true

  server:
    port: 8080
    env: development
#    error:
#      include-message: always
#      include-stacktrace: always

  # local Cache (commented out)
  # datasource:
  #   url: jdbc:h2:mem:orderdb
  #   driverClassName: org.h2.Driver
  #   username: sa
  #   password:

  # H2 (commented out)
  # h2:
  #   console:
  #     enabled: true
  #     path: /h2-console

  # MySQL config - users database
  user:
    datasource:
      jdbc-url: jdbc:mysql://localhost:3306/frog?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC
      username: frog_admin
      password: frog798

  # PostgreSQL config - order database
  order:
    datasource:
      jdbc-url: jdbc:postgresql://localhost:5432/seed
      username: postgres
      password: lego798

  # 邮件配置（开发环境）
  mail:
    host: smtp.qq.com
    port: 587
    username: your-email@qq.com  # 修改为实际的QQ邮箱
    password: your-app-password  # 修改为实际的应用密码
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

# 日志级别配置
logging:
  level:
    root: INFO
